import 'package:get/get.dart';

class HomeBinding implements Bindings {
  @override
  void dependencies() {
    // Initialize controllers needed for home screen
    // Get.lazyPut(() => HomeController());
  }
}